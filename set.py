

 # Set : Unordered Collection of unique items

# Support union and intersection , Remove duplicate

E_set = set()  #syntax
                    # to create empty set where we also declare set with empty
                    # set otherwise it could be considered as a dictionary
                    # because syntax of dictionary is same as it is

print(type(E_set)) # syntax to show data type of any variable

My_set = {1, 2, 3, 4, 5, 3, 6, 7, 8, 9}  # Syntax
                    # To create a multiple disordered element set

print(My_set)
    # Duplicate entity will remove
     #It is same like a List data can be modified in same format
     #as in List
